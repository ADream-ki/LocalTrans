name = "localtrans-privacy-local-only"
version = "0.1.0"
api_version = "1.0"
target_tracks = ["ai_infra"]

[contributes]
workflows = [
  "speech.translate.realtime",
  "speech.asr.whisper",
  "speech.tts.piper",
  "speech.latency.balanced",
  "speech.tts.manual",
  "speech.mode.unidirectional"
]
commands = ["localtrans:workflow-profile/privacy-local-only"]

[core_rewriters]
workflow = true
